public class Problem2 {
    /**We need to keep track of rest list after the operation.
     * Using iteration to do the exchange address of the pointer.
     * 
     * Where to start? From the beginning or the last?
     * If we choose to work from last one,
     * we couldn't go to the end of the list.
     * So tackle it from the beginning.
     * */
/*    
    while (A != null) {
        IntList t1 = A.rest;
        A.rest = B.rest;
        B.rest = t1;
        
        B = A.rest;
        A = t1;*/
    }
    
}
